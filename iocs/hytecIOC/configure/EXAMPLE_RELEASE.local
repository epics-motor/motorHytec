# Use the following lines if motorHytec was built inside motor
MOTOR=
-include $(MOTOR)/configure/RELEASE
# Use the following lines if motorHytec was built outside motor
#!MOTOR_HYTEC=
#!-include $(MOTOR_HYTEC)/configure/RELEASE.local
